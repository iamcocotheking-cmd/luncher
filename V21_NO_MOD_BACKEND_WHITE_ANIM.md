# DURBIN v21 - No DURBIN Mod + Backend + White UI

Changes:
- Removed DURBIN mod installer button from the launcher UI.
- Removed DurbinInstaller.kt from source.
- Replaced the installer button with News + Rank Hub.
- Added the backend dashboard folder:
  DURBIN_BACKEND_DASHBOARD_NETLIFY
- Backend supports:
  - news
  - pvp tier list
  - user rank by UID
  - user rank by Gmail
  - ads
  - admin UID rules
- Added white default text provider in launcher main menu.
- Added white default text provider in Firebase hub.
- Added My Rank fallback:
  1. durbin/userRanks/UID/ranks
  2. durbin/userRanksByEmail/email_key/ranks
- Added more light button press animations:
  - top buttons
  - side rail buttons
  - action cards
  - play button already existed
