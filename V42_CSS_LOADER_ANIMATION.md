# v42 - CSS loader animation

Changed:
- Replaced old loading ring with a Compose version of the uploaded CSS loader.
- Uses three red tilted arcs.
- Animation timing matches the CSS reference:
  - 1.15s linear infinite
  - 3 arcs
  - delay style offsets from the CSS reference
- Background changed to #202628 like the uploaded loader page.
- Text says Loading DURBIN Client...
