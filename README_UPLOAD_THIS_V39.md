# DURBIN Launcher V39

Fixed from the latest log:

- Added missing `ImageReceiver.java` for ModIconCache / ModSearchScreen.
- Added missing native `driver_helper/hook.c`.
- Added missing native `awt_xawt/xawt_fake.c`.

Upload only this version as a full source replacement. Do not mix with old versions.
