# DURBIN v38 - COSA only + LTW + loading animation fix

Changed:
- Fixed the loading circle by replacing it with a real rotating ring animation.
- Removed old two DURBIN mod builds from the COSA/DURBIN page.
- Added only this build:
  https://github.com/iamcocotheking-cmd/luncher/releases/download/cosa/1.21.11.zip
- The build page now shows only:
  DURBIN Client 1.21.11
- Created profile name is now:
  DURBIN Client 1.21.11
- DURBIN Client profile uses LTW renderer:
  opengles3_ltw
- Default renderer is now LTW:
  opengles3_ltw
- Old renderer string `ltw` is migrated to `opengles3_ltw`.
- Home banner detects DURBIN Client profile too.
