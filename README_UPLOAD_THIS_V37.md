# DURBIN V37 Deep Preflight Fixed

Full source replacement. Adds a deeper preflight before build that scans Java/Kotlin and XML resources, creates fallback resources/layouts, and checks source bridge classes before compile.
